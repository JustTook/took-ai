version: '3.8'

services:
  agent-app:
    build: .
    ports:
      - "5001:5000"
    volumes:
      - .:/app
    environment:
      - BACKEND_URL=${BACKEND_URL}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    # .env 파일을 자동으로 로드하지만, 명시적으로 지정할 수도 있음
    env_file:
      - .env
